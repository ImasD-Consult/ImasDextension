{
  "name": "imasd-extension",
  "version": "0.1.0",
  "description": "Trimble Connect extension for ImasD / SmartPrintPRO",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "trimble-connect-workspace-api": "^1.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
