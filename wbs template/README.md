Place WBS template files in this folder.

Current reference template:
- `bSPT_WBS_Template.xlsx` (currently stored at repository root)

Note:
- The Trimble Connect extension runs in the browser and cannot directly write uploaded files to this repository folder at runtime.
- Uploaded files are parsed client-side for preview in the WBS tab.
